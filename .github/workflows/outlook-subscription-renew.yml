name: Outlook Subscription Renew

# Las subscriptions de Microsoft Graph para events tienen TTL máximo de
# ~3 días (4230 minutos). Este cron corre cada 6 horas y le pega al endpoint
# /api/calendar/outlook/subscribe que, por cada conexión:
#   - Renueva la subscription activa si todavía está viva.
#   - Crea una nueva si la subscription expiró (404 al renovar).
#   - Reconcilia: trae el calendarView (−7 / +60 días) y deja cal_events
#     igual a Outlook. Recupera los avisos del webhook que se hayan perdido.
#
# El endpoint usa el header x-cron-secret como auth alterno (no requiere
# sesión de director).
#
# Manual via workflow_dispatch para arranque inicial o re-sync.

on:
  schedule:
    - cron: '0 */6 * * *'
  workflow_dispatch:

jobs:
  renew:
    runs-on: ubuntu-latest
    steps:
      - name: Renovar subscription Outlook
        env:
          DASHBOARD_BASE_URL: ${{ secrets.DASHBOARD_BASE_URL }}
          CRON_SECRET: ${{ secrets.CRON_SECRET }}
        run: |
          set -euo pipefail
          if [ -z "${DASHBOARD_BASE_URL:-}" ] || [ -z "${CRON_SECRET:-}" ]; then
            echo "ERROR: DASHBOARD_BASE_URL o CRON_SECRET no están seteados" >&2
            exit 1
          fi
          response=$(curl -fsS -X POST \
            "${DASHBOARD_BASE_URL}/api/calendar/outlook/subscribe" \
            -H "x-cron-secret: ${CRON_SECRET}" \
            -H "Content-Type: application/json" \
            -d '{}')
          echo "Subscription response: $response"
          # El modo cron devuelve {processed, results:[{user_id,mode,ok,error}]}.
          # Fallar SOLO si alguna conexión erroró de verdad (antes chequeaba
          # .mode, que no existe en modo cron → siempre rojo y enmascaraba fallos).
          processed=$(echo "$response" | jq -r '.processed // 0')
          errors=$(echo "$response" | jq '[.results[]? | select(.ok == false)] | length')
          if [ "$processed" = "0" ]; then
            echo "WARN: no hay conexiones Outlook para renovar"
            exit 0
          fi
          if [ "$errors" != "0" ]; then
            echo "ERROR: $errors conexión(es) fallaron al renovar:" >&2
            echo "$response" | jq -r '.results[]? | select(.ok == false) | "  - \(.user_id): \(.error // "?")"' >&2
            exit 1
          fi
          echo "$response" | jq -r '.results[]? | "  - \(.user_id): \(.mode) · \(.synced // 0) eventos al día, \(.removed // 0) quitados"'
          echo "OK: $processed conexión(es) procesadas, sin errores"
